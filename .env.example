# Optional. The pipeline runs with the rule judge if these are empty.
# Reviewer: copy to .env and add your own keys. Test-set eval does not need them.

OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional path if tesseract is not on PATH (Windows portable build).
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
